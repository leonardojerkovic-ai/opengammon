[package]
name = "og-engine"
description = "Position evaluation, expectimax search, and network inference."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
og-core = { path = "../og-core" }
og-bearoff = { path = "../og-bearoff" }
